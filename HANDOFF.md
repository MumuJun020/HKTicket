# 交接文档

> 给接手这个项目的人（或 AI）看的。README 讲**怎么用**，这份讲**为什么这么写、还差什么**。
>
> 最后更新：2026-08-27

---

## 0. 三十秒了解

Flask 本地控制台 + 比特浏览器指纹窗口 + Playwright，做 hkticketing 的多账号抢票。

```
① 抢票人（账号密码 + 绑窗口）
② 票务解析（纯 HTTP 拿场次票档，不用登录）
③ 配置每个人抢什么
④ 开抢（多账号并发，只锁单不付款）
```

**只锁单，不付款；验证码人工过。** 这两条是设计边界，不要越过。

跑起来：

```bash
cd HKTicket && PORT=5055 ./venv/bin/python run.py
```

改任何解析逻辑之前，先跑：

```bash
./venv/bin/python tests/test_parse_regression.py
```

---

## 1. 接手前必须知道的五件事

### 1.1 解析逻辑改过四轮，每轮都是线上踩坑

失败方式有两种，**「有票报成没票」比报错更糟** —— 它看起来像一个正常结论，
用户完全无从判断是真没票还是程序坏了。四次里有三次是这个方向。

| # | 症状 | 错在哪 |
|---|---|---|
| 1 | 整场售罄报成有票 | 只看票档 `sellOut`，没看场次 `saleState` |
| 2 | 有票报成全部售罄 | 用 `canAddCart` 判可买性（它是"能否加购物车"） |
| 3 | 抢票报"未找到票档" | 页面用 `&nbsp;` 分隔名称和价格，逐字符匹配全灭 |
| 4 | 有票报成全部售罄 | 用 `isPermission` 过滤票档，站点换售卖阶段后全被过滤光 |

**共同的错误模式：拿一个字段在一份快照上推结论，验证吻合就当规律用了。**
`canAddCart` → `isPermission`（判页面显示）→ `isPermission`（判会员码），三次同一个毛病。

现在的防线：

- `tests/test_parse_regression.py`，27 条断言，每个坑一条样本
- `_extract_event()` 里的**安全网**：过滤规则不允许把票档清空，为空就全部保留并打日志
- 界面上的「核对」按钮：打开已登录窗口对着页面逐条比对（见 3.3）

**新遇到解析问题，先往 `tests/fixtures/` 加样本，再改代码。**
最好当场把接口原始返回存进去——事后重建不如真数据可靠。

### 1.2 接口和页面是两个世界，bug 都在接缝上

| | 接口（`ticket_parser`） | 页面（`ticket_operation`） |
|---|---|---|
| 用途 | 配置方案 | 实际抢票 |
| 要登录 | 否 | 是 |
| 要开窗口 | 否 | 是 |
| 开票前可用 | 是 | 否 |

配置用接口给的文本，抢票拿它去页面找元素。**两边对不上就"抢不到"，而且表现得像"没票"。**

已知的对不上：

- 页面名称和价格之间是 `&nbsp;`（U+00A0），接口是普通空格
- 页面票档名前有 `<span class="gaTag___rlXCy">站</span>` 角标，`innerText` 会拼进来
- 会员预售期接口返回 12 个票档，页面只显示 6 个（父项带 `childPriceIds`，页面不显示父项）

匹配统一走 `_normalize_text()` + 包含关系，不要用 Playwright 的 `has_text`。

### 1.3 站点会按 IP 限流，被封过一次

2026-08-13：为了验证接口字段做了二三十次**纯 API 连打**（不加载页面资源、没有埋点），
整个出口 IP 被拉黑，站点所有域名 `ERR_CONNECTION_CLOSED`，持续很久。

三道防护**不要拆**：

- `ticket_api.MIN_API_INTERVAL = 3.0` 强制间隔
- 解析全程只发一个请求
- 抢票循环默认 2~4 秒 + 限流识别 + 指数退避
- 蹲回流票**阶梯降频**（前 2 分钟 2~4s → 15 分钟内 8~15s → 之后 30~60s）
  蹲一晚上从 ~9600 次降到 ~730 次

### 1.4 一个窗口只能绑一个抢票人

两个账号绑同一个窗口时，并发任务会操作同一个浏览器、互相覆盖填的账号密码。
更隐蔽的是**换人复用窗口不清登录态**：程序检测到"已登录"就跳过登录，
然后拿上一个人的账号去抢这个人配置的票——**下单成功，但下到了错的人头上，全程不报错**。

`data/window_owner.json` 记录窗口归属，换人时自动清登录态。三层校验都在，别拆。

### 1.5 比特浏览器同步接口必须走 `asyncio.to_thread`

`openBrowser` 等函数内部是同步 `requests`，一次几秒。直接在协程里调会卡住事件循环，
让 `asyncio.gather` 的并发退化成串行。实测 2 个窗口：阻塞 4.9s vs `to_thread` 2.7s。

---

## 2. 代码结构

```
app/Auto/
  bit_api.py           比特浏览器本地 API（127.0.0.1:54345）
  ticket_parser.py     ② 纯 HTTP 解析场次票档，不开窗口不登录
  ticket_login.py      一键登录 + 登录态检测 + 登录态清除
  ticket_operation.py  ④ 抢票引擎，全程 UI 自动化
  ticket_verify.py     用页面内容核对解析结果（只读）
  ticket_store.py      JSON 持久化 + 启动清理
  ticket_api.py        页面上下文里的只读接口调用（带节流）
app/Controllers/ticket.py   全部 HTTP 接口
app/templates/index.html    单页控制台（原生 JS，无框架）
tests/                      解析回归测试 + 样本
```

数据都在 `data/`（已 gitignore）：

| 文件 | 内容 | 启动时 |
|---|---|---|
| `accounts.json` | 抢票人，**明文密码** | 清空 |
| `event.json` | 解析结果 | 清空 |
| `plans.json` | 每人抢什么 | 清空 |
| `window_owner.json` | 窗口上次登录的是谁 | 清空 |
| `results.json` | 抢到的订单 | **保留** |

`results.json` 刻意不清——它是流程的产出，锁到的订单还等着人去付款。

---

## 3. 已经做完的

### 3.1 主流程
账号管理（增删改查、批量、CSV/Excel 导入、模板下载、窗口自动分配）、
一键登录（并发、跳过已登录、验证码人工）、票务解析、每人独立配置、
立即/定时抢票、暂停恢复停止、蹲回流票、锁单、SSE 实时日志。

### 3.2 抢票结果
`data/results.json` 按批次记录锁到的订单，含订单号抓取（三条兜底路径）、
支付状态人工标记、CSV 导出（UTF-8 BOM + 订单号防科学计数法）。

**抓不到订单号也要记**——抓取依赖页面结构，但"这个人抢到了"比订单号更重要，
丢掉整条会让用户以为没抢到而错过付款。

### 3.3 防错
- **开抢前自检**（`/ticket/preflight`）：逐人检查窗口/登录态/配置，红绿灯
- **核对**（`/ticket/verify_event`）：打开已登录窗口进选票页，把页面真实票档和
  解析结果逐条比对，列出「接口有页面没有」「页面有接口没有」「售罄判断对不上」。
  **全程只读**，不调数量不点下一步不下单
- 刷新页面后自动接管仍在跑的任务（服务端状态为准，不依赖 localStorage）
- 限购上限（`eventOrderLimit`）在下拉、数量框、自检三处提示

### 3.4 会员优先购票码
账号可选填。抢票时点「下一步」若弹出输入框，自动填入并点确定。
码不对**不重试**——确定性失败，重试结果一样，而且反复提交错码容易被风控盯上。

---

## 4. 已知没做 / 待办

### 4.1 🔴 排队没有实现（最重要的缺口）

**真实的热门场抢票流程是：**

```
点「立即购买」→ ⏳ 排队（可能很久）→ 进选票页看到票 → 选择 → 提交订单 → 跳转支付
                 ↑↑↑ 这一段完全没实现 ↑↑↑
```

目前测试用的都是票量充足的活动，点「立即购买」直接进选票页，所以没遇到排队。
**真正的热门场（开票瞬间几万人抢）几乎一定会排队**，现在的代码会怎么表现是未知的。

现在会发生什么（推测，未验证）：

- `grab_ticket_on_page` 点完「立即购买」后等 `.sellTicketContent___PIY_T`，超时 5 秒
- 排队页多半不含这个选择器 → 抛 `RuntimeError("选票页未就绪")` → 本轮失败 → 等 2~4 秒重试
- **重试会重新走一遍流程，很可能把已经排到一半的队重置掉**，永远排不进去

接手要做的事，按顺序：

1. **先拿一个真的会排队的活动去看**，把排队页的 DOM 抓下来：路由是什么、
   有没有位次/预计时间元素、进入选票页时页面怎么变。没有这份观察就别写代码，
   这个项目已经因为"照着一份快照推规则"栽过四次
2. **识别排队态**：在 `grab_ticket_on_page` 第 3 步之前判断是不是在排队页
3. **排队时不要重试**，安静等着，把位次打进日志（用 `[COUNTDOWN]` 前缀，
   前端会原地刷新那一行，不刷屏）
4. 排队期间仍要响应暂停/停止（参考 `_wait_if_paused_or_stopped`）
5. 给排队设超时上限，别无限等

代码落点：`app/Auto/ticket_operation.py` 的 `grab_ticket_on_page`，第 2 步和第 3 步之间。

### 4.2 🔴 蹲票没有总时长上限

`max_attempts` 默认 0 = 无限，任务永远不会自己结束。忘了停就一直跑。
建议加「最多蹲 N 分钟」并给个非无限默认值（比如 60 分钟）。
「蹲 30 分钟」比「试 600 次」直观得多——后者你根本不知道要跑多久。

### 4.3 🟠 抢到票没有提醒

锁单后只有**五分钟**付款时限，但抢中只在日志滚一行 + 顶栏数字变。
人不在电脑前就完全错过。加个声音提示（一行 `new Audio(...).play()`）或浏览器通知。

### 4.4 🟠 从未验证过的

| 项 | 情况 |
|---|---|
| 真正的多窗口并发抢票 | 全程只有 1 个账号跑过完整流程，5 窗口同时抢是推算 |
| 自动填会员码的**成功**路径 | 弹窗出现和取消测过，「填入→确定→通过」没有真码可测 |
| 排队 | 见 4.1 |
| 比特浏览器的并发上限 | 只有 5 个窗口，测不出套餐/客户端是否有限制 |

### 4.5 🟡 体验

- 日志只在内存，刷新就没（已可导出 MD，但不自动落盘）
- 启动清空账号，每次重启要重新录入（有 CSV 导入和 `KEEP_DATA=1` 缓解）
- 窗口的「名字」和「序号」容易混（界面已显示成 `窗口「1」· 序号4`，
  但导入时填数字仍可能歧义，建议把窗口命名成非数字）

---

## 5. 性能与容量

实测（16GB M 系列 Mac）：

```
每个比特浏览器窗口约 1.7 GB 内存 → 16GB 实际能开约 5 个
窗口冷启动 1.6s，并发开 2 个也是 1.6s（不排队）
已开的窗口再调 openBrowser：0.002s（几乎免费）
```

**开抢前先把窗口都开好登录好**，`openBrowser` 那一步的耗时就没了。

100 个窗口需要 100+ GB 内存，不是软件限制，是物理限制。

---

## 6. 不要做的事

| | 为什么 |
|---|---|
| 验证码自动识别 | 识别失败率在关键时刻更被动；性质从"辅助操作"变成"绕过风控" |
| 自动支付 | 明确的设计边界，只做到锁单 |
| 砍掉请求节流 | 已经被封过一次 IP，被封了是彻底抢不了 |
| 自动填实名信息 / 跨账号复用身份 | 项目明确不存实名信息，守住这条 |
| 用接口直接下单绕过 UI | 页面状态会脱节；有些参数是上一步接口返回的；风控看行为轨迹 |

---

## 7. 排查手册

| 现象 | 先看哪 |
|---|---|
| 解析结果和页面对不上 | 点「核对」按钮；跑 `tests/test_parse_regression.py` |
| 抢票报"未找到票档" | 日志里会列出页面上实际有哪些票档，对比配置 |
| 抢票一直"本次尝试未成功" | 看是不是排队（4.1）；看登录态；看是否被限流 |
| 窗口列表空 / 连不上 | 顶部红色横幅会说明；比特浏览器客户端要开着且登录 |
| 站点所有页面打不开 | IP 被限流了，换 IP 或等 |
| 明明改了配置却没生效 | 配置是自动保存的，但换活动后旧配置会被校验拦下，重新配 |

---

## 8. 提交历史里的关键节点

```
8da8351  抢票工具主体
4dd0160  清理公开仓库敏感文件 + README 教程
4eab5fe  抢票结果记录 + CSV 导出
d2e4b41  修复刷新页面丢失任务
03079fb  可用性改进（布局锁定、步骤编号、自检、CSV 导入、蹲票降频）
c1676c9  修票档匹配（&nbsp;）
e13a85c  会员预售场支持（会员码关卡、数量默认 0、限购）
2659375  修「有票报成全部售罄」（isPermission 过滤）
fdb4a7c  解析回归测试
a29f760  修「公售中误标需会员码」+ 核对功能
```

每个提交信息里都写了**为什么**，不只是改了什么。排查历史问题时值得读。
