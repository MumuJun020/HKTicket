# 解析测试样本

每个文件是一份**真实或按实测记录重建**的接口返回，对应一次真实踩过的坑。

`nct127_公开发售中.json` 是直接从接口抓的原始数据（只裁掉了解析用不到的字段）。
其余是按当时记录下来的字段值重建的——原始响应没留下来，但关键字段的取值
在代码注释里有明确记录，重建出来的样本能复现同样的判断分歧。

**新遇到解析问题时，先在这里加一个样本，再改代码。**
只改代码不加样本的话，下一次数据形态变化会把同一个坑再踩一遍——
这个项目的解析逻辑已经因此改了四轮。
